version: '3.4'

services:
  {{ serviceNameAndRelativePath }}:
    image: {{ serviceNameAndRelativePath }}
    build:
      context: .
      dockerfile: Dockerfile
{{#if ports}}
    ports:
{{#each ports}}
      - {{ . }}:{{ . }}
{{/each}}
{{/if}}
