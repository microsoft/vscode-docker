name: Node CI Build and Test

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build_and_test:
    uses: microsoft/vscode-azuretools/.github/workflows/main.yml@bmw/ghpipelines
